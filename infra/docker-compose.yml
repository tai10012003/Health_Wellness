services:
  postgres:
    image: postgres:17-alpine
    container_name: health_wellness_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: health_wellness
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d health_wellness"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
